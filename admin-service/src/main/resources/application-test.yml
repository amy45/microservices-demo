server:
  port: 9000
spring:
  application:
    name: ADMIN-SERVICE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka
  instance:
    metadata-map:
      instance-id: admin